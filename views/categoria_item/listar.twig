		<a class='enlace' href="?agregar">Agregar categoria item</a>
					<table id="users-table">
						<thead>
						<tr>
								<th>Descripcion</th>
								<th>Categoria padre</th>
								<th>Borrado?</th>
						</tr>
						</thead>
						<tbody>
							{% for categoria in categorias | slice(inicio,itemsxpag) %}
								<tr><td>{{ categoria.getDescripcion() }}</td>
								{% if padres[categoria.getId()] != null %}
									<td>{{ padres[categoria.getId()].getDescripcion() }}</td>
								{% else %}
									<td>(Ninguna)</td>
								{% endif %}
								{% if categoria.getBorrado() == 1 %}
									<td>Si</td>
								{% else %}
									<td>No</td>
								{% endif %}
								<td><a class='enlace' href='?editar={{ categoria.getId() }}'>Editar</a></td></tr>
							{% endfor %}																																																																																																																																	
						</tbody>
					</table>
					{% for i in range(1, cantpag) %}
					    <a class='enlace' href="?pagina={{ i }}">{{ i }}</a> 
					{% endfor %}
					{% if hayMensaje == true %}
						<div> Mensaje:  "{{ mensaje }}"</div>
					{% endif %}			